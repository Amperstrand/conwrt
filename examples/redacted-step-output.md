# Step-01 Discovery Report — Linksys Velop WHW03 v2

**Run ID**: `20260101-120000-velop-mesh`
**Operator**: `example-operator`
**Date**: 2026-01-01T12:00:05Z

---

## 1. Network Sweep

A directed ARP scan of 192.168.1.0/24 via `eth0` confirmed `192.168.1.1`
on the local broadcast domain. The MAC address was `E8:9F:80:XX:XX:XX`;
the OUI `E8:9F:80` is registered to **Linksys LLC** (Belkin International),
consistent with Linksys Velop mesh networking hardware.

A secondary device was detected at `192.168.1.2` with MAC `E8:9F:80:XX:XX:XX`,
indicating a second mesh node on the same network.

## 2. Port Scan Results

A targeted TCP SYN scan of `192.168.1.1` revealed the following open ports:

| Port  | Service  | Notes                   |
|-------|----------|-------------------------|
| 80    | HTTP     | Redirects to HTTPS      |
| 443   | HTTPS    | Web management interface|
| 2200  | JNAP     | Linksys proprietary API |
| 8080  | HTTP     | Firmware update endpoint|

Port 2200 is the **JNAP** protocol port used by Linksys Velop nodes for
inter-node communication and device identification.

## 3. HTTP / HTTPS Fingerprinting

Connecting to `https://192.168.1.1` returned the Linksys Velop login page,
loading assets from `linksyssmartwifi.com`. The device presented a
self-signed certificate with CN `E8:9F:80:XX:XX:XX`.

The HTTP redirect chain was:

```
http://192.168.1.1 → https://192.168.1.1
                    → https://linksyssmartwifi.com/ui/1.0/login.html
```

Response headers confirmed the device identity:

```
Server: nginx/1.16.1
X-Device-Model: WHW03
X-Firmware-Version: 1.1.12.213490.prod
```

## 4. JNAP Device Identification

A JNAP `Identify` request was sent to `https://192.168.1.1:2200/JNAP/`.
The device responded with the following identification payload:

```json
{
  "output": {
    "friendlyName": "My Velop Mesh",
    "modelName": "WHW03",
    "hardwareVersion": "v2.0",
    "firmwareVersion": "1.1.12.213490.prod",
    "macAddress": "E8:9F:80:XX:XX:XX",
    "serialNumber": "<REDACTED:SERIAL>",
    "nodeType": "primary",
    "meshRole": "master",
    "isConnected": true,
    "internetConnection": "connected",
    "wanIpAddress": "<REDACTED:PUBLIC-IP>"
  }
}
```

Key observations from the JNAP response:

- **Model**: WHW03 v2 — second revision of the AC2200 Velop node
- **Mesh Role**: This node is the **primary/master** in the mesh topology
- **Firmware**: Version 1.1.12.213490.prod (production build)
- **Serial**: `<REDACTED:SERIAL>` — redacted per policy
- **WAN IP**: `<REDACTED:PUBLIC-IP>` — external IP redacted for privacy

## 5. DNS and Hostname Analysis

The device's DHCP hostname was observed as `<REDACTED:HOSTNAME>` on the
local network. It also responds to `linksyssmartwifi.com` via its internal
DNS resolver. Internal DNS resolution showed:

```
192.168.1.1   → <REDACTED:HOSTNAME>
192.168.1.2   → <REDACTED:HOSTNAME>-node-2
```

## 6. Mesh Topology

The JNAP `GetMeshInfo` endpoint returned details about the mesh network:

- **Primary node**: `192.168.1.1` (MAC `E8:9F:80:XX:XX:XX`)
- **Child node 1**: `192.168.1.2` (MAC `E8:9F:80:XX:XX:XX`)
- **Backhaul**: Wireless (5 GHz band), RSSI -42 dBm

## 7. Conclusion

The target device at `192.168.1.1` is confirmed as a **Linksys Velop WHW03 v2**
tri-band mesh Wi-Fi node operating as the primary/master node in a 2-node
mesh deployment. Confidence level: **confirmed**.

Evidence supporting this identification:

1. MAC OUI `E8:9F:80` matches Linksys LLC registration
2. JNAP `Identify` response confirms model WHW03 v2
3. HTTP headers confirm model and firmware version
4. Web interface matches known Linksys Velop UI
5. Management portal loads from `linksyssmartwifi.com`

---

*Report generated by conwrt step-01 discovery module. All sensitive
identifiers have been redacted per the conwrt redaction policy.*
